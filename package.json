{
  "name": "hotelsonweb",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "build:shared": "npm run build -w shared",
    "build:backend": "npm run build -w backend",
    "dev": "npm run build:shared && npm run dev -w backend & npm run dev -w frontend",
    "vercel-build": "npm run build:shared && npm run build -w frontend"
  }
}
